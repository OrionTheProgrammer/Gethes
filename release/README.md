# Release Artifacts

This directory keeps only release notes under `release/notes/`.

Binary artifacts (`.zip`, `.exe`, checksums) are generated locally by `build_exe.ps1`
and should be published through GitHub Releases instead of being committed.
